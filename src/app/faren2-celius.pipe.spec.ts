import { Faren2CeliusPipe } from './faren2-celius.pipe';

describe('Faren2CeliusPipe', () => {
  it('create an instance', () => {
    const pipe = new Faren2CeliusPipe();
    expect(pipe).toBeTruthy();
  });
});
